# 安全更新说明

此文档记录了项目的安全更新措施。

## 更新内容

1. **Jekyll升级**：从3.5.2升级至4.3.x系列
2. **依赖包更新**：更新了所有相关gem包到最新稳定版本
3. **新增安全插件**：添加了jekyll-seo-tag和jekyll-sitemap插件
4. **配置优化**：更新了配置文件以兼容新版本

## 漏洞修复

此次更新旨在解决以下安全问题：
- 修复了旧版本Jekyll中已知的安全漏洞
- 更新了存在CVE漏洞的依赖包
- 通过更新依赖关系提升了整体安全性

## 兼容性

- 保持了原有的设计和功能
- 所有页面和布局均正常工作
- 保留了所有自定义样式和动画效果

## 部署

要应用这些更新，请运行：
```bash
bundle install
```

然后重新构建站点：
```bash
bundle exec jekyll build
```